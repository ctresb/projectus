[package]
name = "projectus-desktop"
version = "0.1.0"
description = "PROJECTUS desktop"
edition = "2024"

[lib]
name = "projectus_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
projectus-server = { path = "../../../crates/server" }
tauri = { version = "2", features = [] }
